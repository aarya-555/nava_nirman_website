Fonts (Bricolage Grotesque, Inter, Space Mono) are loaded from Google Fonts via
the <link> in index.html, so no local font files are required.

To self-host instead, download the families into this folder and replace the
Google Fonts <link> in index.html with local @font-face rules in css/style.css.
